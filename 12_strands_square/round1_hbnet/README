Design the folding mortise-tenon motif using the HBNet application. Here, 2 different motif positions are sampled. 

For each input backbone, run the following command:
while read line ; do cd $line/ && /software/rosetta/latest/bin/rosetta_scripts -s ../$line.pdb -beta_nov16 -parser:protocol ../hbnet.xml && cd .. ; done < task.list

Analyze the output using the get_all_motifs.py script.
